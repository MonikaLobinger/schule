#! /usr/bin/env bash
if [ -f .ispriv ]; then
  rm .ispriv
  mv .stutiis XXXstutiis
  sleep 1
  touch .isexport
  .erstelleleeremitschriften
else if [ -f .isexport ]; then
  .erstelleleeremitschriften
  echo "Bereits im Export Modus"
else
  echo "Exportiert werden kann nur im  Private mode"
fi
